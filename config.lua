Config = {
    debug = true, -- Set to true to enable notifications, false to disable
    disableFog = true, -- Set to true to disable fog weather
    updateInterval = 60000, -- Interval for updating time and weather (ms)
    weatherChangeInterval = 600000, -- Interval for requesting weather updates (ms)
}
